import React from 'react';
import styled from 'styled-components';

const SideBarWrapper = styled.div`
  grid-area: sidebar;
  background-color: #6666d3;
`;

const SideBar = () => {
  return <SideBarWrapper>S</SideBarWrapper>;
};

export default SideBar;
